#include "main.h"

/**
 * _memset - put ablock of memory with value
 * @n: integer number and character and array of string
 * @dest: is array of string
 * @src: is pointer of character
 * Return: return array of changed value on success
*/
char *_memcpy(char *dest, char *src, unsigned int n)
{
	int i;

	for (i = 0; n > 0; i++, n--)
	{
		s[i] = b;
	}




	return (s);
}
