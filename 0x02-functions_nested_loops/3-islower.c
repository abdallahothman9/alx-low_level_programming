# include "main.h"

/**
 * _islower - checks if letter is lower case or capital case
 *
 * @c: The character argument
 *
 * Return: returns 1 if the number is positive. 0 otherwise
*/

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
