#include "main.h"
/**
 * _abs - make negative oositivw number
 *
 * @n:number negative be positive
 * Return: return n after make it positive
**/
int _abs(int n)
{
	if (n < 0)
		n = n - 1 * n;
	return (n);
}
